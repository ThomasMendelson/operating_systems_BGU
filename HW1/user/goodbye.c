#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"

int
main(int argc, char *argv[])
{
    //exit(0,0);
    exit(0,"Goodbye World xv6");
    //exit(0,"Goodbye World xv6 and make it longer now more the 32 have to be very long must be longer still not long enough");
}